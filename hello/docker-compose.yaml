# create docker-compose nginx for hosting hw.html

version: '3.8'

services:
  nginx:
    image: nginx:latest
    ports:
      - 8080:80
    volumes:
      - ./hw.html:/usr/share/nginx/html/index.html

